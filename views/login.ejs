<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="icon" type="image/x-icon" href="/images/FAVICON.ico">
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha1/css/bootstrap.min.css"
    />
    <style>
      @font-face {
        font-family: 'relieve';
        src: url("/fonts/Relieve.ttf");
      }

      body.card-title{
        font-family: 'relieve';
      }


      body {
        background: url("images/wallpaper.png");
      }
      .card {
        color: #5f13ca;
        position: relative;
        z-index: 1;
        background: #ffffff;
        border-radius: 5px;
        padding: 20px;
        text-align: center;
      }
      .card {
        width: 500px; /* Set the width of the form box */
        min-height: 200px; /* Set the height of the form box */
      }

      .container {
        max-width: 750px;
        margin-top: 120px;
      }

      .card:before {
        content: "";
        position: absolute;
        z-index: -1;
        top: -16px;
        right: -16px;
        bottom: -16px;
        left: -16px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 5px;
      }
      .form-control {
        position: relative;
        box-sizing: border-box;
        height: auto;
        padding: 10px;
        font-size: 16px;
      }
      .form-check-label {
        display: block;
      }
      .btn-purple {
        background-color: #7c5c7e; 
        border: none;
        border-radius: 20px; 
        color: white;
        padding: 10px 25px; /* Adjust the padding to increase or decrease button size */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional: Adds a subtle shadow effect */
        font-size: 16px;
        text-align: center;
        display: inline-block;
        width: 100%; /* This will make the button expand to the full width of its parent container */
        -webkit-transition: background-color 0.3s ease;
        transition: background-color 0.3s ease;
      }

      .btn-purple:hover,
      .btn-purple:focus {
        background-color: #6a4f6b; /* Slightly darker shade for hover/focus */
        color: white; /* Keep text color the same */
      }
    </style>
  </head>
  <body>
    <div
      class="container d-flex align-items-center justify-content-center h-100"
    >
      <div class="card">
        <div class="card-body">
          <h3 class="card-title">Login</h3>
          <script>
    // Check for error query parameter and show an alert if present
    if (new URLSearchParams(window.location.search).has('error')) {
      alert('Invalid email or password. Please try again.');
    }
  </script>
          <p>Dont have an account? <a href="/register">Register</a></p>
          <form action="/api/users/login" method="post">
            <div class="mb-3">
              <input
                type="email"
                class="form-control"
                name="user_email"
                placeholder="Email"
                required
              />
         
            <div class="mb-3">
              <input
                type="password"
                class="form-control"
                name="user_pass"
                placeholder="Password"
                required
              />
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-purple">Continue Judging</button>
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha1/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
